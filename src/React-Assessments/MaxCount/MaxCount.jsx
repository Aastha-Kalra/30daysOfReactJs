import React from "react";

const MaxCount = () => {
  return (
    <div>
      <div>MaxCount</div>
      <div>
        Implement a button and timmer that will start from 10 and ends at 0 and
        tracks the counter untill the timer expires and button should disapper
        once timer expires
      </div>

      <div>No. of clicks until timer expires</div>
    </div>
  );
};

export default MaxCount;
